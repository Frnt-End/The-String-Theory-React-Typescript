import React from "react";


export default function ArrList() {

 
  return (
  
[
      "slice(0, 3)", "charAt(4)", "concat('..', '!')", "includes('e')",
      "length-1", "substr(4, 3)", "replace(' ', '🧃')", "toUpperCase()",
      "padStart(24,'🍭')", "split('')", "indexOf('F')", "lastIndexOf('F')",
      "search('oo')", "match(/f/gi)", "search(/f/gi)", "startsWith('Foo')",
      "repeat(2)","substring(5)", "substr(0, 9).concat('..😉')"
]
)
}